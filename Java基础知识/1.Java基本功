1.1Java入门

    1.1.1关于 JVM JDK 和 JRE 最详细通俗的解答

        JVM是Java虚拟机,JVM 有针对不同系统的特定实现（Windows，Linux，macOS），目的是使用相同的字节码，它们都会给出相同的结果.这样就实现了跨平台(JVM 可以理解的代码就叫做字节码（即扩展名为 .class 的文件）)
        JDK 是 Java Development Kit 缩写，即Java开发套件
        JRE 是 Java 运行时的环境。它是运行已编译 Java 程序所需的所有内容的集合，包括 Java 虚拟机（JVM），Java 类库，java 命令和其他的一些基础构件。但是，它不能用于创建新程序。
        JDK包含JRE,JRE包含JVM

1.2Java语法疑问

    1.2.1标识符和关键字的区别是什么？

        标识符就是一个被你命名过的名字,比如你定义一个sum函数,sum就是标识符.
        关键字是系统自己给出的命名,比如public,是不能再被你命名的,比如你定义一个public函数就会定义不了.
    1.2.2Java泛型

        泛型就是一种参数化类型,比如你先定义一个student类,然后使用ArrayList <student>来新建ArrayList,这样的ArrayList就只能存储student类而不能存放其他类(这种泛型使用方式称为泛型类)
        泛型一般有三种使用方式:泛型类、泛型接口、泛型方法。
        分别对应:public class Generic<T>      public interface Generator<T>     public static < E > void printArray
        通配符:常用的通配符为： T，E，K，V，？

        ？ 表示不确定的 java 类型
        T (type) 表示具体的一个java类型
        K V (key value) 分别代表java键值中的Key Value
        E (element) 代表Element

    1.2.3. hashCode()与 equals()
        hashCode介绍:hashCode() 的作用是获取哈希码，也称为散列码；它实际上是返回一个 int 整数(hashCode与对象是多一对应的,对象相同hashCode相同,但hashCode相同对象不一定相同)。这个哈希码的作用是确定该对象在哈希表中的索引位置。hashCode() 定义在 JDK 的 Object 类中，这就意味着 Java 中的任何类都包含有 hashCode() 函数。
   
1.3Java数据类型
     8种基本数据类型
        6种数字类型 ：byte、short、int、long、float、double
        1种字符类型：char
        1种布尔型：boolean
     
